const projectTemplates = ['next', 'x0']

// TODO: Update this to use kit repo when this is made public
const tarUrl = 'https://codeload.github.com/mdx-js/mdx/tar.gz/master'

module.exports = { projectTemplates, tarUrl }
