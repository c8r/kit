{
  "private": true,
  "name": "@compositor/kit",
  "description": "Components for development environments, style guides, and demos",
  "version": "1.0.0",
  "license": "MIT",
  "repository": "c8r/kit",
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "format": "prettier --no-semi --single-quote --write '{docs,packages,core,cli}/**/*.js'",
    "prepare": "lerna bootstrap && npm run clean && npm run build",
    "start": "x0 dev docs/Demo.js",
    "test": "lerna run test"
  },
  "devDependencies": {
    "@compositor/x0": "^4.0.1",
    "lerna": "^2.5.1",
    "prettier": "^1.12.1"
  }
}
